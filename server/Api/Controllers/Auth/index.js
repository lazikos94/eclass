const Login = require('./Login');
const Register = require('./Register');
const CheckToken = require('./CheckToken');
const GetRoles = require('./GetRoles');

module.exports = { Login, Register,CheckToken,GetRoles};